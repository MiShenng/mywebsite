# Task Plan: Clean up Hugo site repo + fix CV bullet alignment

## Goal
(1) Make the repository structure clean and easy to extend without breaking the live
Hugo site (relative paths / deploy must keep working). (2) Fix the LaTeX CV so the
bullet at the start of each project/entry aligns with its sub-item bullets.

## Context (verified facts)
- Hugo static site. Theme = `hugo-ht` git submodule (currently NOT initialized — empty).
- `baseURL = https://www.chiselli.com/`. Deploy: `vercel.json` pins HUGO_VERSION=0.122.0,
  so **Vercel rebuilds from source**. `public/` (15M, 192 files) is ALSO committed
  (legacy deploy flow). `.gitignore` says public/ is intentionally tracked.
- CV PDFs: `cv/*.pdf` are the source; `static/media/{english,chinese}_cv.pdf` are
  **symlinks** -> `../../cv/*.pdf`. A `.claude/sync-cv.sh` hook copies them.
- Content references: `/media/english_cv.pdf`, `/media/chinese_cv.pdf`,
  `/media/pdfs/*.pdf` (absolute site paths -> served from `static/`).
- LaTeX CV uses macros: outer list `\resumeSubHeadingListStart` (itemize leftmargin=*),
  entries `\resumeProject`/`\resumeSubheading` emit `\item`+tabularx; inner bullets
  via `\resumeItemListStart`. Misalignment = outer entry bullet vs inner item bullet.

## Mess inventory (P-ranked)
- P0 stray ROOT loose files that don't belong / confuse:
  - `.Rhistory` (empty), `.hugo_build.lock` (build lock), `texput.log` (stray LaTeX log),
    `update.sh` (duplicate of deploy.sh), `notes_deploy.md` (personal note).
- P0 stray dir clutter (untracked, local-only): `.DS_Store` everywhere,
  `cv/.claude/` (duplicate session logs), `.claude/logs/` session md files,
  `cv/*.aux|fls|fdb_latexmk|xdv|log` build artifacts (already gitignored, just local).
- P1 structure: CV build artifacts live next to source in `cv/`; could isolate.
- P1 `public/` 15M committed but Vercel rebuilds → likely redundant (NEEDS USER OK).
- P1 themes submodule not initialized → `git submodule update --init` needed for local build.

## Key Decisions (RESOLVED by user 2026-06-09)
1. `public/` — **leave tracked, do not touch** (will verify Vercel deploy method but not remove).
2. Deploy scripts — **delete `update.sh`, keep `deploy.sh`** (consolidate).
3. `notes_deploy.md` — **move to `docs/`**.
4. Reorg aggressiveness — **Structural**: docs/ for notes, cv/build/ for CV artifacts,
   scripts/ for shell scripts. Move dirs but keep all site-referenced paths intact.

## Target structure (Structural reorg)
```
/ (root)
  config.toml, README.md, vercel.json, .gitignore, .gitmodules   <- stay (Hugo/Vercel need them)
  archetypes/ assets/ content/ data/ i18n/ layouts/ static/ themes/  <- Hugo dirs, untouched
  public/                                                          <- left as-is (tracked)
  docs/            <- NEW: notes_deploy.md (-> docs/deploy-notes.md)
  scripts/         <- NEW: deploy.sh moved here; .claude/sync-cv.sh stays (hook path)
  cv/
    *.tex, *.pdf, photo-for-cv.png   <- source stays
    build/         <- NEW: .gitignore'd; CV aux/log/fls/etc compile here (local only)
```
Constraints: deploy.sh references must still run from repo root; if moved to scripts/,
update notes + any caller. Hugo never imports root scripts, so safe. `static/media/*cv.pdf`
symlinks point to `../../cv/*.pdf` — must stay valid (cv/*.pdf not moving).

## Phases
- [x] Phase 0: Survey repo, understand Hugo layout, deploy flow, CV macros
- [x] Phase 1: Confirm scope + risky decisions with user (public/, aggressiveness)
- [x] Phase 2: Fix CV bullet alignment — DIAGNOSED + FIXED + VERIFIED
      - Bug was ENGLISH-CV-ONLY. Cause: `leftmargin=*` made entry-bullet x-position
        depend on each item's tabularx body → entry bullets drifted between sections
        (Education entries at x~73 vs Publication entries at x~85, ~2.3mm off).
      - Fix: pinned 3 list macros to fixed leftmargin (3.4mm outer / 3.6mm inner) in
        english_cv.tex lines 123-128. Verified via pixel measurement + side-by-side
        render (build/compare_bullets.png): all entry bullets now share one column,
        all sub-item bullets share one column.
      - chinese_cv.tex NOT changed: uses \entryhead headings (no entry bullets) +
        uniform global leftmargin=1.4em → already aligned. Confirmed by render.
      - TODO: copy fixed build/english_cv.pdf -> cv/english_cv.pdf (site source).
- [x] Phase 3: Clean clutter — deleted local-only artifacts (cv/*.aux|log|fls|xdv|out|fdb,
      root .Rhistory/.hugo_build.lock/texput.log, dup cv/.claude/). None were tracked.
      Hardened .gitignore (cv/build/). .DS_Store left (regenerated by macOS, already ignored).
- [x] Phase 4: Structural tidy — docs/ (deploy-notes.md, repo-structure.md), scripts/deploy.sh
      (hardened to cd repo-root), removed update.sh. sync-cv.sh kept in .claude/ (hook).
- [x] Phase 5: Verified. Theme submodule initialized. `hugo` build = 105 pages, 0 errors.
      Live server: /, /en/about/cv/, /media/english_cv.pdf, /en/posts/, /media/pdfs/bai.pdf
      all 200; CV pdf served as application/pdf. ALL DONE.

## Path-dependency findings (CRITICAL for reorg safety)
- `.claude/sync-cv.sh` is a REGISTERED PostToolUse hook in `.claude/settings.local.json`
  (`$CLAUDE_PROJECT_DIR/.claude/sync-cv.sh`). MUST stay in `.claude/`. NOT a user script.
- `.vscode/settings.json`: `livePreview.serverRoot=/public` → confirms public/ used for
  local preview. Leave public/ alone (matches user choice).
- `.vscode/tasks.json`: Hugo dev server task, cwd=workspaceFolder. No script-path deps.
- `deploy.sh` has no automated caller (only documented in notes_deploy.md). Safe to move to
  scripts/. Will harden it to `cd` repo-root first so `git add .` works from anywhere.
- Vercel: vercel.json pins HUGO_VERSION=0.122.0 → builds from source. Confirmed indirectly.

## Reorg execution list (FINAL)
- mkdir docs/ scripts/ ; cv/build/ already exists
- git mv notes_deploy.md docs/deploy-notes.md
- git mv deploy.sh scripts/deploy.sh  (+ harden: cd to repo root)
- git rm update.sh  (duplicate, user-confirmed delete)
- .gitignore: add cv/build/ ; add /public/ note unchanged (keep tracked)
- copy fixed english_cv.pdf done; cv/build/ artifacts are local-only (gitignored)
- DO NOT move: sync-cv.sh (hook), any content/ static/ layouts/ Hugo dir, public/, cv/*.pdf
- Add docs/repo-structure.md documenting the layout for future content additions

## Errors Encountered
- (none yet)

## Round 2 (2026-06-09, follow-up requests)
User reported 4 items after round 1:
1. 把中文cv清理掉 → FULL removal (user-confirmed). Removed: cv/chinese_cv.{tex,pdf},
   static/media/chinese_cv.pdf (symlink), content/cn/about/CV.md. Edited .claude/sync-cv.sh
   to drop chinese_cv. Updated docs/ (repo-structure.md, deploy-notes.md) to match.
   Verified: /cn/about/cv/ and /media/chinese_cv.pdf now 404; 0 dangling refs in build.
2. 点的位置还是不对 → REAL bug found: round-1 fixed-margin made entry bullets share a
   column, BUT long titles wrap to 2 lines and the `\item` bullet aligned to the tabularx
   VERTICAL CENTER → bullet dropped to the 2nd line. FIX: added `[t]` top-alignment to
   tabularx in \resumeSubheading + \resumeProject (english_cv.tex ~L102,L111). Verified by
   strip render: every entry bullet now on line 1 of its title, all in one column.
   Re-copied corrected PDF to cv/english_cv.pdf.
3. content/en/2026-5-8/index.html → DELETED. Plus its only inbound link was a test post
   content/en/posts/2021-01-07-test.md (author "Your Name", category test, slug second-post)
   whose sole content was the jump link → user confirmed delete BOTH. No dead link remains.
4. "可" (ack).
Hugo build after round 2: 96 pages, 0 errors. Live: kept pages 200, removed pages 404.

## Status
**ALL DONE (both rounds).** Repo restructured (docs/ scripts/ cv/build/), Chinese CV fully
removed, English CV bullet alignment fixed (fixed margins + [t] top-align), stray page +
test post deleted. Site builds clean (96 pages); CV serves; removed URLs 404 as expected.
NOT yet committed — left for user (note: english_cv.tex also carries user's own prior edits
to award labels / page-break penalties; committing will include those).
